/** Minimum buy (wei) for First Smart Buy — matches mission text (0.01 BNB). */
export const FIRST_SMART_BUY_MIN_WEI = 10n ** 16n;
/** Cumulative trading volume (BNB) for Volume Monster — matches missions UI. */
export const VOLUME_MONSTER_MIN_BNB = 1;
